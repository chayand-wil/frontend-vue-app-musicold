user:
   id
   username
   email
   password
   user_status enum('active','ban')

article:
   id
   id_publicacion
   id_music_genre
   id_artist
   type enum('vinyl', 'cds', 'cassettes')
   quantity 
   category enum('nuevo', 'semi-usado', 'usado', '7_plg', '8_plg','9_lg', 'special_edition')
   price
   color
   publication_year
   dimension_x
   dimension_y

songs:
   id
   id_article
   song_name
   side enum('Lado_a','Lado_b')

 
wishlist:
   id_user
   id
   id_publication

rating:
   id
   id_user
   id_publication
   score

comment:
   id
   id_user
   id_publication
   id_comment_status enum('visible','hidden','ban')
   created_at DATETIME

publication:
   id
   id_publication_type
   image
   status enum('is_presale', 'available', 'hidden', 'sold_out')
   audio
   updated_at DATETIME
   created_at DATETIME

publication_type:
   id
   description

promotion:
   id
   id_promotion_type
   start_date
   end_date

promotion_type:
   id
   description
   discount
   max_limit

event:
   id
   id_artist
   id_publication
   title
   start_date

artist:
   id
   name

user_attendance:
   id_publication
   id
   id_user

chat_comment:
   id_publication
   id
   id_user
   created_at DATETIME

order:
   id
   id_user
   id_publication
   updated_at DATETIME
   created_at DATETIME
   quantity
   state enum('completed', 'shipped', 'ready_to_shipped')

music_genre:
   id
   description
   slug

notification:
   id
   id_publication
   id_user
   detail

promotion_articles:
   id_article
   id
   id_promotion


